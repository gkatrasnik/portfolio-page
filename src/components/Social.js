import * as React from "react";
import { Card } from "react-bootstrap";
const Social = (props) => {
  return (
    <Card style={{ width: "5rem" }} className="m-2">
      <Card.Body>
        <Card.Text>Social</Card.Text>
      </Card.Body>
    </Card>
  );
};

export default Social;
